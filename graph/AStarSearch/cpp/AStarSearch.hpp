#pragma once

#include <string>
#include <vector>

using namespace std;

int shortestPathLengthAStar(const vector<string>& grid,
                            int startRow,
                            int startCol,
                            int goalRow,
                            int goalCol);
